<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.happyhouse.model.mapper.MapMapper">

	<select id="getDongPosition" parameterType="baseAddr" resultType="baseAddr">
		select city, dongcode, gugun, dong, lat, lng from baseaddress
		<where>
			city = #{city}
			and
			gugun = #{gugun}
			and
			dong = #{dong}
		</where>
	</select>
	
	<select id="getAptPosition" parameterType="baseAddr" resultType="houseDto">
		SELECT dong, aptName, code, buildYear, jibun, lat,lng FROM houseinfo
			where code = (
			select distinct dongcode
			from baseaddress
			where city = #{city}
			and gugun = #{gugun}
			)
			and dong = #{dong};
	</select>
	
</mapper>